# Инструкция по запуску приложения BirQadam

## 📋 Текущая конфигурация сети

Ваши IP-адреса:
- **Wi-Fi**: `10.128.2.68` (используйте этот для реальных устройств)
- **VirtualBox**: `192.168.56.1` (только для виртуальных машин)

## 🚀 Шаг 1: Запуск Django сервера

Откройте терминал в папке `BirQadamDjango` и выполните:

```powershell
cd BirQadamDjango
.\benv\Scripts\activate
python manage.py runserver 0.0.0.0:8000
```

Сервер будет доступен по адресам:
- `http://127.0.0.1:8000` (локально)
- `http://10.128.2.68:8000` (для мобильных устройств в той же сети)
- `http://10.0.2.2:8000` (для Android эмулятора)

## 📱 Шаг 2: Запуск Flutter приложения

### Вариант A: На Android эмуляторе

```powershell
cd BirQadamApp
flutter run
```

Приложение автоматически подключится к `http://10.0.2.2:8000`

### Вариант B: На реальном Android устройстве

```powershell
cd BirQadamApp
flutter run --dart-define=API_URL=http://10.128.2.68:8000
```

⚠️ **Важно**: Убедитесь, что мобильное устройство подключено к той же Wi-Fi сети, что и компьютер!

### Вариант C: На iOS симуляторе

```powershell
cd BirQadamApp
flutter run --dart-define=API_URL=http://127.0.0.1:8000
```

### Вариант D: На реальном iOS устройстве

```powershell
cd BirQadamApp
flutter run --dart-define=API_URL=http://10.128.2.68:8000
```

⚠️ **Важно**: Убедитесь, что iPhone подключен к той же Wi-Fi сети, что и компьютер!

## 🔍 Проверка подключения

Перед запуском приложения проверьте, доступен ли Django сервер:

1. Откройте браузер на компьютере: `http://127.0.0.1:8000`
2. Если сервер работает, вы увидите страницу Django
3. Если не работает - проверьте логи в терминале

## ❌ Решение проблем

### Проблема: "Connection refused" или "Failed to connect"

**Решение:**
1. Убедитесь, что Django сервер запущен на `0.0.0.0:8000`
2. Проверьте брандмауэр Windows - разрешите порт 8000
3. Убедитесь, что устройство и компьютер в одной Wi-Fi сети
4. Проверьте IP адрес командой: `ipconfig` и используйте актуальный Wi-Fi IP

### Проблема: "CORS error"

**Решение:**
IP адрес уже добавлен в `CORS_ALLOWED_ORIGINS` в `settings.py`. Если проблема остается, убедитесь что:
- `DEBUG = True` в `.env` файле
- Сервер перезапущен после изменений

### Проблема: Приложение не запускается

**Решение:**
```powershell
cd BirQadamApp
flutter clean
flutter pub get
flutter run --dart-define=API_URL=http://10.128.2.68:8000
```

## 📝 Быстрая команда для запуска

Создайте файл `start_app.ps1` в корне проекта:

```powershell
# Запуск Django сервера в фоне
Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd BirQadamDjango; .\benv\Scripts\activate; python manage.py runserver 0.0.0.0:8000"

# Запуск Flutter приложения
Start-Sleep -Seconds 3
cd BirQadamApp
flutter run --dart-define=API_URL=http://10.128.2.68:8000
```

Запустите: `.\start_app.ps1`





