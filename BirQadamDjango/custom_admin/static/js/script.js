document.addEventListener('DOMContentLoaded', () => {
    console.log('Dashboard loaded');
    // Если используются графики (например, Chart.js), добавьте их инициализацию здесь
    // Пример: 
    // const ctx = document.getElementById('project-chart').getContext('2d');
    // new Chart(ctx, { type: 'bar', data: {...}, options: { responsive: true } });
});